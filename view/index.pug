extends layout

block content
    if(session.user != null)
        p Session = #{session.user.userName}
        a(href="/logout", class="button") Logout
        a(href="/editUser/" + session.user.userID, class="button") Edit Profile
        a(href="/createPost/" + session.user.userID, class="button") Make Post
        if(session.user.isAdmin == 'true')
            a(href="/data", sclass="button") data
    else
        a(href="/login", class="button") Login
    each item in post
        div(class="post")
        if(session.user != null)
            if(item.ownerID == session.user.userID)
                a(href="/editPost/" + item.id, class="button") Edit
            if(session.user.userID == item.ownerID || session.user.isAdmin == 'true')
                a(href="/deletePost/" + item.id) delete
        div(class="postTableInfo")
            p(id="postInfo") #{item.ownerID}
            p(id="postInfo") #{item.ownerName}
            p(id="postInfo") #{item.date}
            p(id="postInfo") #{item.message}
            img(src="profiles/" + item.ownerAvatar, alt=item.ownerName)

    p(id="users") #{user}
    canvas(id="canvasId")
    <script src="barGraph.js"></script>
