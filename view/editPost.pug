extends layout
block content
    form(id="form", method="post", action="/editPost/" + post.id)
        label Message:
        input(class="textBox", type="text", name="message", value = post.message)
        br
        input(class="textBox", type="submit", value="Update Post")